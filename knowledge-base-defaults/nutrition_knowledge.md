# Nutrition Knowledge — reference · SKELETON

> Committed skeleton, **not** real content. Overridden by `/knowledge-base/nutrition_knowledge.md`
> (local, gitignored). Reference context only — the app computes calorie/carb/protein targets
> deterministically (`lib/nutrition.ts`); **the AI never calculates nutrition**, it only phrases the
> pre-computed values.

## In-ride fuelling
_(stub — carbohydrate g/hr guidance scaled by ride duration + intensity.)_

## Race week & race day
_(stub — carb-load (8–12 g/kg) in the 36–48 h before; pre-race meal timing; in-race g/hr; caffeine.)_

## Daily intake
_(stub — base + activity kJ + buffer on training days, flat target on rest days — the deterministic
formula owns the numbers; this is narrative context only.)_
